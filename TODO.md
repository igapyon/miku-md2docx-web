# TODO

- Decide whether upstream should publish a browser-compatible runtime release
  asset distinct from the CLI bundle. Until then, `refresh:runtime` uses the
  current local main-application runtime path, `dist/core.js`.
- Enable GitHub Pages for this separated `-web` repository and publish
  `index.html` from the default branch.
- Recheck parity with the upstream `miku-md2docx` browser behavior after the
  first separated Web release.
