# TODO

- Decide the upstream release asset contract for the browser-compatible
  `miku-md2docx` runtime and update `vendor/miku-md2docx-runtime.json` with the
  release tag and asset URL.
- Enable GitHub Pages for this separated `-web` repository and publish
  `index.html` from the default branch.
- Recheck parity with the upstream `miku-md2docx` browser behavior after the
  first separated Web release.
